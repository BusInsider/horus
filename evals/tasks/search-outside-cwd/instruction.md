# Task: Search in Another Directory

Search for all TypeScript interfaces in `/home/jackm/.hermes/workspace/horus/src` using the grep tool.

The search should find interface definitions like `interface Something {`.
