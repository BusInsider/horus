# Task: Fix the Calculator and Verify

The file `calculator.js` has a bug. The `multiply` function is currently adding the numbers instead of multiplying them.

1. Fix the `multiply` function in `calculator.js`
2. Run `node test-calculator.js` to verify your fix
3. If the tests fail, fix the bug again and re-run the tests until they pass

Do not stop until `node test-calculator.js` outputs "All tests passed!".
