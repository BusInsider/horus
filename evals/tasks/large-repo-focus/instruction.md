# Task: Find the Shipping Cost Function

In the `monorepo/` directory, there is a large codebase with over 1000 files.

Find the file that contains the `calculateShippingCost` function and write its **relative path from the workspace root** to `answer.txt`.

For example, if the function is in `monorepo/packages/utils/shipping.ts`, write exactly:
```
monorepo/packages/utils/shipping.ts
```

Use the most efficient search strategy. Do not read every file.
