# Task: Refactor JavaScript to TypeScript

The file `old_code.js` contains legacy JavaScript. Refactor it to TypeScript:
1. Add proper type annotations
2. Convert to ES module syntax (import/export)
3. Add proper error handling
4. Save as `refactored.ts`
