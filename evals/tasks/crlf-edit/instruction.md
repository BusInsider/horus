# Task: Fix the Config File

The file `config.txt` has Windows-style line endings (CRLF). Replace the line that says:

```
DEBUG=true
```

with:

```
DEBUG=false
```

Leave all other lines unchanged.
