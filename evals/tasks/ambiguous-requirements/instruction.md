# Task: Build the Notification System

Create a notification system for the app. It should handle different kinds of notifications and work well.

Create ALL files directly in the current working directory. Do NOT use a `src/` subdirectory.
