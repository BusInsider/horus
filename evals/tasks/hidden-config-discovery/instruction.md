# Task: Find the Hidden Configuration

Explore the current workspace directory to find all files. There is a hidden configuration file that contains important settings.

Read the contents of the hidden configuration file and write them exactly to `found-config.txt`.

Do not guess the filename — examine the directory contents first.
